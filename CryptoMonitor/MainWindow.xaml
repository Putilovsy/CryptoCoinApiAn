<Window x:Class="CryptoMonitor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        Title="CryptoMonitor" Height="500" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BgDark}">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Column="0" Grid.Row="0" Margin="5" 
                Background="{StaticResource PanelDark}" 
                CornerRadius="8" 
                BorderBrush="{StaticResource BorderColor}" 
                BorderThickness="1"
                ClipToBounds="True">
            <DataGrid ItemsSource="{Binding Coins}"
                      SelectedItem="{Binding SelectedCoin}"
                      AutoGenerateColumns="True"
                      BorderThickness="0"
                      Background="{StaticResource PanelDark}"/>
        </Border>

        <Button Content="Анализ динамики"
                Grid.Row="1"
                Grid.Column="0"
                Margin="5,10,5,5"
                Command="{Binding OpenAnalysisCommand}"
                FontSize="14" FontWeight="SemiBold"/>

        <Border Grid.Column="1" Grid.RowSpan="2" Margin="10,5,5,5"
                Background="{StaticResource PanelDark}"
                CornerRadius="8"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="1">
            <Grid>
                <ScottPlot:WpfPlot x:Name="CryptoPlot" Margin="10"/>

                <Button x:Name="ToggleChartBtn"
                        Content="📊 В свечи"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="0,15,15,0"
                        Padding="15,6"
                        FontSize="12"
                        Background="#2B3655"
                        Click="ToggleChartBtn_Click"/>
            </Grid>
        </Border>

        <Border Grid.ColumnSpan="2" Grid.RowSpan="2"
                Background="#B30B132B"
                Visibility="{Binding IsLoadingCoins, Converter={StaticResource BoolToVisibility}}">
            <Border Background="{StaticResource PanelDark}"
                    CornerRadius="12"
                    Padding="25,20"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    BorderBrush="{StaticResource BorderColor}"
                    BorderThickness="1"
                    Effect="{DynamicResource DropShadowEffect}">
                <StackPanel Orientation="Vertical">
                    <ProgressBar IsIndeterminate="True" Width="150" Height="4" Margin="0,0,0,15"
                                 Foreground="{StaticResource AccentHover}" 
                                 Background="{StaticResource BgDark}"
                                 BorderThickness="0"/>
                    <TextBlock Text="{Binding LoadingMessage}" 
                               HorizontalAlignment="Center" 
                               Foreground="{StaticResource TextMain}" 
                               FontSize="14"/>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</Window>